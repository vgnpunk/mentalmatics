package de.vegnpunk.mentalmatics.data.di

import org.koin.dsl.module

/**
 * Koin module for `:data` (ADR-007): empty for now, populated once the
 * SQLDelight driver/repositories exist (settings in #4, progress in #13).
 */
val dataModule = module {}
