package de.vegnpunk.mentalmatics.core.di

import org.koin.dsl.module

/**
 * Koin module for `:core` (ADR-007): empty for now, populated as
 * `:core` gains injectable dependencies (e.g. task generators wired
 * per practice session).
 */
val coreModule = module {}
